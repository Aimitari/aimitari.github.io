# AImitari landing page

- all files published need to be in the src folder
- used vite to build the site
- used mb5bootstrap for UX

## publish

```bash
npm run build
git add --all
git commit -m 'Witty comment'
git push origin main
```
